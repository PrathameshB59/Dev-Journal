<script src="/js/auth.js"></script>
<script src="/js/app.js"></script>
<% if (typeof extraScripts !== 'undefined' && extraScripts.length) { %>
    <% extraScripts.forEach(function(s) { %>
        <script src="/js/<%= s %>"></script>
    <% }); %>
<% } %>
